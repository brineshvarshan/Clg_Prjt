apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "secure-ecommerce.serviceAccountName" . }}
  labels:
    {{- include "secure-ecommerce.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
